# Example environment configuration for acme-gateway

# Server port
PORT=8080

# JWT secret for validating tokens from acme-auth-service
JWT_SECRET=your-secret-key

# Rate limiting (requests per second)
RATE_LIMIT_PER_SECOND=100

# Backend service URLs
TICKETS_SERVICE_URL=http://localhost:8081
